---
import { getCollection } from "astro:content";

const documents = await getCollection("documents");
---

<aside
  class="border border-brand-700 bg-opacity-10 bg-brand-300 rounded-lg py-5 px-9"
>
  <h2 class="text-2xl mb-4">Documents</h2>
  <ul>
    {
      documents.map(doc => (
        <li class="mb-2 underline text-lg">
          <a href={doc.data.url} target="_blank" rel="noopener noreferrer">
            {doc.data.title}
          </a>
        </li>
      ))
    }
  </ul>
</aside>
